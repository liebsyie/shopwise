import * as t from '@babel/types';
import { Plugin } from 'vite';
type Options = {
    debug?: boolean;
};
export declare function canvasMode(opts?: Options): Plugin;
export declare function transformJSX(srcCode: string, filePath: string): import("@babel/generator").GeneratorResult;
export declare function addSourceAttributes(srcCode: string, filePath: string): {
    ast: t.File;
};
export declare function replaceTextAndStripSourceAttributes(elementId: string, text: string): {
    code: string;
    filePath: string;
};
export declare function stripSourceAttributes({ ast }: {
    ast: t.File;
}): {
    ast: t.File;
    code: string;
};
export {};
